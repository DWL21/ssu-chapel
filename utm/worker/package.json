{
  "name": "ssu-chapel-utm-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ssu-chapel-utm --file=schema.sql",
    "db:init:remote": "wrangler d1 execute ssu-chapel-utm --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
