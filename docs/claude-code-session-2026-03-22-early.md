# Claude Code 작업 내역 — 2026-03-22 (오전 세션)

## 개요

2026년 3월 22일 오전 세션. 인증 토큰 기반 구조로 전환하고 좌석 뷰어 및 미니맵을 구현한 대규모 리팩토링 세션.

---

## 1. 인증 토큰 기반 구조로 전환

**관련 커밋** `1328e10` `2621436` `2444c39` `6e1ea4d`

**내용**
- `/auth/token` 엔드포인트로 SSO 인증을 분리, 이후 요청은 토큰만 사용
- 채플 조회 요청에서 user ID 제거 (토큰만으로 처리)
- 학년도/학기를 현재 날짜 기준으로 자동 산정 (9월 이후 → 2학기)
- 인증 실패 시 자동 재시도 로직 추가
- `localStorage`로 토큰 영속화 (새로고침 후 자동 로그인)

---

## 2. 로그아웃 및 KV 캐시 연동

**관련 커밋** `9cbc8bd`

**내용**
- 로그아웃 버튼 추가, `/auth/logout` 호출로 KV 캐시 무효화
- `wrangler.toml`에 `CHAPEL_AUTH_CACHE` KV 네임스페이스 바인딩 추가
- 로그아웃 시 localStorage 토큰/userId 제거

---

## 3. 출결 현황 표시 개선

**관련 커밋** `6af2af5`

**내용**
- 총 세션 수 대신 필요 출석 횟수(80% 기준) 표시로 변경
- 통과 기준 충족 / 결석 한도 도달 / 한도 초과 상태 색상 구분

---

## 4. 좌석 뷰어 컴포넌트 구현

**관련 커밋** `75a5754` `95cafd9` `d7c9de6` `bb906a8` `bac7bca` `8a1a920` `d1b7b95` `bc38ecb`

**내용**
- `SeatViewer` 컴포넌트 신규 구현
  - 구역(section) / 행(row) / 열(col) 파싱 및 표시
  - 좌석 번호 파싱 함수 `parseSeat()` 구현 (`"G - 8 - 2"` 형식 지원)
- 좌석 배치 그리드(`SeatSectionGrid`) 구현: 내 좌석 ★ 표시
- 2층/3층 복도 구분선 표시
- 파비콘 업데이트, 배경색 조정
- 불필요한 좌석 위치 텍스트 제거, 컴포넌트 props 단순화

---

## 5. ChapelMiniMap 및 SectionBrowser 구현

**관련 커밋** `1030942` `3267bb7` `c338f48` `aa9087c`

**내용**
- `ChapelMiniMap` 컴포넌트: 1층/2·3층 구역 버튼 미니맵
  - 내 구역 하이라이트, 클릭으로 다른 구역 좌석 배치 탐색
- `SectionBrowser` 컴포넌트: 비로그인 상태에서 구역별 좌석 배치 탐색
- 층별 라벨 너비 고정, 텍스트 줄바꿈 방지 수정

---

## 6. 비밀번호 해싱 및 KV 캐시 구현

**관련 커밋** `e3f42b9` `282ee38`

**내용**
- Worker에서 비밀번호 SHA-256 해싱 후 캐시 키로 사용
- 동일 인증 정보 재요청 시 KV에서 토큰 반환 (SSO 서버 부하 감소)
- 코드 구조 전반 리팩토링: 함수 분리 및 가독성 개선
- Swagger UI 및 OpenAPI 스펙 (`/docs`, `/openapi.json`) 엔드포인트 추가
