# Cloudflare Pages 배포 시 이 변수를 대시보드에서 설정:
#   VITE_API_BASE=https://ssu-mails-worker.<your-account>.workers.dev
# 로컬 개발은 기본값(http://127.0.0.1:8787)을 사용하므로 보통 비워둬도 됨.
VITE_API_BASE=http://127.0.0.1:8787
